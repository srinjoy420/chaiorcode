// exports.add = function (a, b) {
//     return a + b;
// };

// exports.sub = function (x, y) {
//     return x - y;
// };

const add=function (a, b) {
    return a + b;
}

const sub = function (x, y) {
    return x - y;
};

// default expoet

// default expoet
module.exports= {
   add,
   sub
}